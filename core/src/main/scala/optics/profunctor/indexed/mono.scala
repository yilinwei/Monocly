package profunctor.optics.mono

// import profunctor.optics.{core => C}

// type Lens[A, B] = C.Lens[Any, Nothing, A, A, B, B]
// type Prism[A, B] = C.Prism[Any, Nothing, A, A, B, B]
// type Adapter[A, B] = C.Adapter[Any, Nothing, A, A, B, B]
// type Affine[A, B] = C.Affine[Any, Nothing, A, A, B, B]
// type Traversal[A, B] = C.Traversal[Any, Nothing, A, A, B, B]
// type Getter[A, B] = C.Getter[Any, Nothing, A, A, B, B]

// type GetterE[E, A, B] = C.Getter[Any, E, A, A, B, B]
// type LensE[E, A, B] = C.Lens[Any, E, A, A, B, B]


// object Test {

//   val lens: LensE[Int, String, Int] = ???
//   val traversal: Getter[Int, Boolean] = ???

//   val get: GetterE[String | Int, String, Boolean] = lens >>> traversal

//   traversal.get(32)

// }
